export interface OpenWindow {
  id: string;
  title: string;
  tab: string;
}

export interface ExperienceItem {
  company: string;
  role: string;
  period: string;
  description: string;
}

export type Theme = 'xp' | '95';
